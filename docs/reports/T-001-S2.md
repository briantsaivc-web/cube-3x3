# 回報：T-001 game-engineer（S2 engine 方塊模型）

| 欄位 | 內容 |
|---|---|
| 角色 | game-engineer |
| 任務 | T-001（S2：engine 方塊模型 — rng、cube、scramble） |
| 日期 | 2026-09-15 |
| 結論 | 完成 |

## 1. 結論（3 行內）

- 依 `game-spec.md` §1、§2.1、§2.4、§3.1–§3.3、§5、§9.2 第 3 點，新寫 `src/engine/rng.js`、`cube.js`、`scramble.js`，全部純函數、無時鐘／DOM／亂數依賴。
- `tests/engine/cube.test.js`（T-ENG-01～04、06～10、25）與 `tests/engine/scramble.test.js`（T-ENG-11）共 11 項測試全數通過；種子 20260915 的打亂字串與規格範例逐字相符。
- `check-forbidden.sh` 通過（exit 0）；無簡體字；未讀寫 `src/data/`、`src/solver/`、`src/ui/` 或其他段負責的檔案，無阻礙。

## 2. 變更檔案清單

| 檔案（相對路徑） | 動作 | 行數 ＋／－ | 備份檔 |
|---|---|---|---|
| `src/engine/rng.js` | 新增 | +25 | — |
| `src/engine/cube.js` | 新增 | +440 | — |
| `src/engine/scramble.js` | 新增 | +57 | — |
| `tests/engine/cube.test.js` | 新增 | +251 | — |
| `tests/engine/scramble.test.js` | 新增 | +55 | — |
| `docs/reports/T-001-S2.md`（本檔） | 新增 | — | — |

## 3. 測試證據

- 指令：`node --test tests/engine/cube.test.js tests/engine/scramble.test.js`
- 輸出摘要：

```
# Subtest: T-ENG-01：36 個記號各做 4 次回原狀（X2 做 2 次） ... ok
# Subtest: T-ENG-02：X 後 X′ 回原狀、X′ 後 X 回原狀、X2 = X·X（20 個隨機狀態） ... ok
# Subtest: T-ENG-03：恆等式 M=RL′x′、E=UD′y′、S=F′Bz、x=RM′L′、y=UE′D′、z=FSB′ ... ok
# Subtest: T-ENG-04：方向抽查（U 帶 F 頂排到 L；M 帶 U 中欄到 F；E 帶 F 中排到 R；S 帶 U 中排到 R） ... ok
# Subtest: T-ENG-06：qtmCost（外層 1／2、中層 2／4、整顆 0，36 個逐一比對） ... ok
# Subtest: T-ENG-07：isSolved（24 朝向的復原狀態皆 true；M R′ L 後為 true；單一 M 後為 false） ... ok
# Subtest: T-ENG-08：24 種朝向互不相同；編號順序固定；ROTATE 只改 orient ... ok
# Subtest: T-ENG-09：toHome／toView 互逆，且 view 做 X＝home 做 toHome(o,X)（2,000 組隨機） ... ok
# Subtest: T-ENG-10：gestureToMove（216 組貼紙位移與滑動同向；§9.2 抽樣表逐項相符） ... ok
# Subtest: T-ENG-25：describeMove（36 個記號逐一比對；′ 的三種寫法結果相同） ... ok
# Subtest: T-ENG-11：打亂規則（同 seed 相同、範例字串、無同面相鄰、無同軸連三、長度、非復原、seed 檢查） ... ok
# tests 11
# pass 11
# fail 0
# duration_ms 約 240ms
```

- `bash .claude/hooks/check-forbidden.sh; echo $?` → exit code `0`（無非決定性來源或 DOM／計時器依賴字串，含註解）。
- 未測試的項目與原因：`T-ENG-05`（外層置換與 `solver/twophase.js` 的 `applyMoveFacelets` 對照）依分派單由 S4 負責，本段不寫；本段亦未跑 `npm test` 全套（`src/solver/`、`src/ui/`、`build/` 尚未由對應段完成，跑了也會失敗於非本段範圍）。

## 4. 未預期發現

- 無。

## 5. 需要製作人決定的事

- 無。

## 6.（選填）需要其他角色配合的事

- **24 種朝向編號已依規格 BFS 規則（生成元 x→y→z、依發現順序編號）鎖定，往後任何段（S3、S4、S5、S6）不得改變此編號**（分派單 X-2）。鎖定值如下（供後續段核對）：
  - `orientAfter(0, 'x')` = `1`
  - `orientAfter(0, 'y')` = `2`
  - `orientAfter(0, 'z')` = `3`
  - 24 個朝向置換的雜湊（`sha1(applyOrient(SOLVED,o).join(',')).slice(0,12)`，o=0…23）已寫入 `tests/engine/cube.test.js` 的 `LOCKED_ORIENT_HASHES` 常數，供其他段或未來重構比對回歸。
- `describeMove`、`inverse`、`kind`、`qtmCost`、`toHome`、`toView`、`layerStickers`、`gestureToMove`、`parseMoves`、`formatMove` 均已完成並凍結（分派單 X-2、X-18），S8a 的 `notation.js` 可直接呼叫 `describeMove` 取得白話說明所需的 `base`／`kind`／`turn`／`qtm`。
