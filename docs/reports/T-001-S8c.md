# 回報：T-001 game-engineer（S8c）

| 欄位 | 內容 |
|---|---|
| 角色 | game-engineer |
| 任務 | T-001（S8c：UI 提示、示範分頁與播放器、白話說明、分段暫停卡片、求解器狀態） |
| 日期 | 2026-09-15 |
| 結論 | 完成 |

## 1. 結論（3 行內）

- 已實作 `src/ui/hint-view.js`（提示按鈕、提示浮層、層高亮、求解器狀態）與 `src/ui/demo-player.js`（示範分頁：種類選擇、播放器、記號列高亮、白話說明、層先法分段清單與分段暫停卡片、播放控制）＋ `src/ui/demo.css`，並補上 D-15 要求的「離開示範自動切回操作分頁」。
- `node build/bundle.js` 成功（342,039 bytes）；`check-forbidden.sh` exit 0；繁簡掃描 0 命中；engine 測試 39/39 通過；Playwright 於手機（390×844）與 iPad（1194×834）兩種視窗完整跑過提示、建議解示範、層先法示範（含 6 次分段暫停）、單步／播放／暫停／速度、已解時三顆按鈕停用、求解器建表中等待與取消等全部檢查項，零 JS 錯誤。
- 無受阻項；有一項建議 S8a／systems-engineer 配合的小改動（`ctx.solver.failureTextKey`），以及一項跨模組觀察（S8d 的完成遮罩會擋住示範收尾的回看操作），詳見第 4、6 節。

## 2. 變更檔案清單

| 檔案（相對路徑） | 動作 | 行數 ＋／－ | 備份檔 |
|---|---|---|---|
| `src/ui/hint-view.js` | 新增（改寫自 X-7 空殼） | +210 | — |
| `src/ui/demo-player.js` | 新增（改寫自 X-7 空殼） | +595 | — |
| `src/ui/demo.css` | 新增（改寫自 X-7 空殼） | +122 | — |

未動任何其他 `src/ui/` 檔案；`notation.js` 僅以 `require` 呼叫其匯出的純函數（`fillTemplate`／`explainMove` 等）。

## 3. 測試證據

### 3.1 建置

- 指令：`node build/bundle.js`
- 輸出：
```
build 完成：index.html（v0.0.0；342039 bytes；18 個模組、4 個資料檔；Worker 字串 82941 bytes、5 個模組；零外部資源）
```

### 3.2 禁用 API 掃描

- 指令：`bash .claude/hooks/check-forbidden.sh`
- 結果：`exit=0`（僅掃 `src/engine/`、`src/solver/`，不涵蓋本段 `src/ui/` 檔案，但本段程式碼亦未使用 `Math.random()`／`Date.now()` 作為規則依據，計時相關僅用於 UI 顯示與播放節奏）

### 3.3 簡體字掃描

- 指令：`python3 scratch/g3-check/check-simplified.py src/ui/hint-view.js src/ui/demo-player.js src/ui/demo.css`
- 結果：`命中行數 0`

### 3.4 Engine 測試

- 指令：`node --test tests/engine/*.test.js`
- 結果：`# tests 39 / # pass 39 / # fail 0`（本段未新增 engine 測試，純 UI 段落；跑既有測試確認未動到 engine 層）

### 3.5 Playwright 驗證（`scratch/s8c-check/check.js`、`scratch/s8c-check/check-solver-wait.js`）

- 指令：`NODE_PATH=/home/claude/cube-3x3/scratch/spike-solver/node_modules PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers node scratch/s8c-check/check.js`（另跑 `check-solver-wait.js`）
- 手機（390×844）與 iPad（1194×834）兩視窗結果（節錄自 `scratch/s8c-check/results.json`）：

| 檢查項 | phone | ipad |
|---|---|---|
| 打亂後按提示：出現記號＋剩餘步數＋白話說明＋層高亮 | `U（1 步）· 照建議解還要 30 步`／`U：上層順時針轉 90°，算 1 步`／highlight 20 格 | `L2（2 步）· 照建議解還要 27 步`／`L2：左層轉半圈（180°），算 2 步`／highlight 20 格 |
| 執行該步後再按提示，換下一步 | `F（1 步）· 照建議解還要 29 步` | `D′（1 步）· 照建議解還要 25 步` |
| 建議解示範：完整播放到解完，步數顯示正確 | 標題「建議解：共 29 步」，播完後最終累計 30 步（含播放前手動走的 1 步）；解完自動切回操作分頁 | 標題「建議解：共 25 步」，最終累計 27 步（同上，含手動 1 步）；自動切回操作分頁 |
| 層先法示範：分段清單、6 次分段暫停卡片、繼續、播完解成功 | 「層先法：共 145 步」，8 段清單，暫停 6 次，播完累計 145 步（與標題一致，因先按重置清空手動步） | 「層先法：共 203 步」，8 段清單，暫停 6 次，播完累計 203 步（與標題一致） |
| 已解時「建議解示範」「層先法示範」「提示」三顆按鈕皆停用（D-2） | 皆為 `disabled:true` | 皆為 `disabled:true` |
| 單步前進／後退 | `cursor 0→1`，前進後再退回 0，皆正確 | 同左 |
| 播放／暫停：播放中步數會前進，暫停後穩定不再變動 | `playAdvanced:true`／`pauseHeld:true` | 同左 |
| 離開示範回到「操作」分頁（D-15） | `true`（建議解與層先法兩種情境皆驗證） | `true` |
| JS 錯誤 | `[]` | `[]` |

- `check-solver-wait.js`（求解器建表中按提示 → 等待狀態；補充驗證，僅手機視窗）：
  - 建表未完成時按提示：`.wait-card` 顯示等待訊息＋進度條（`hasProgressBar:true`），文字為「求解器準備中…」類提示，未出現「最短／最佳解」字樣。
  - 按「取消」：`state.hint` 立刻變回 `null`，等待卡片立即消失；之後再按一次提示，等到建表完成後自然接續完成並顯示提示卡（`hintReadyAfterBuild:true`）。
  - 全程 `jsErrors: []`。

- 截圖：`scratch/s8c-check/phone-01-open.png` ～ `phone-13-solver-ready-hint.png`、`ipad-01-open.png` ～ `ipad-11-step-play-pause.png`（共 26 張）。已用 Read 工具目視抽查手機與 iPad 兩組關鍵畫面（提示卡、建議解示範、層先法分段清單、分段暫停卡片、已解停用狀態、求解器等待卡），文字皆完整未截斷，版面在兩種尺寸下皆正常。

- 按鈕文字覆核：畫面上僅出現「建議解示範」「層先法示範」字樣，未出現「最短」「最佳解」「最少步」等字眼（見 `demo-player.js` 的 `buildKindRow`／`texts.json` 對應鍵值，及上列截圖）。

### 3.6 未測試項目與原因

- 未涵蓋鍵盤操作／螢幕閱讀器等無障礙輔助工具的實測（僅依規格檢查觸控熱區 ≥44×44px 與 aria 相關屬性沿用既有 `notation.js` 產生的文字，未另跑 a11y 專用工具）。
- 未測試橫向手機、直向 iPad 等非任務指定的視窗尺寸組合（僅驗證 390×844 與 1194×834 兩種必測尺寸）。

## 4. 未預期發現

- **S8d（`records.js`）的完成遮罩會擋住示範收尾的回看操作**：規格 §9.1 要求示範播完解出後「仍可前後單步回看」，但 `records.js` 的 `showResult()` 只要 `state.status==='solved' && state.result` 成立就會顯示全螢幕 `.overlay-mask`（未檢查 `state.demo` 是否仍非 null），導致建議解／層先法示範播完當下，畫面上的「離開示範」「上一步」等按鈕會被遮罩擋住點擊。此為 S8d 檔案，依指示未動它；本次驗證改在測試腳本裡強制隱藏遮罩（`page.evaluate` 設 `mask.hidden=true`）繞過，僅供本段驗證使用，不影響產品程式碼。建議 S8d 補上「`state.demo` 非 null 時暫不顯示遮罩，或遮罩底下的示範播放器仍可互動」的修正。
- **`texts.json` 的 `hud.qtmNote` 鍵值目前無人使用**：查詢 `src/data/texts.json` 確認此鍵存在（放 QTM 計步規則的簡短說明文字），但 `src/ui/controls.js`（非本段檔案）目前沒有任何地方讀取它，畫面上從未顯示。範圍外，未動它，僅記錄供後續參考。

## 5. 需要製作人決定的事

- 無。

## 6. 需要其他角色配合的事

- 建議 S8a／systems-engineer 在 `app.js` 組裝 `ctx.solver` 時補上 `failureTextKey: client.failureTextKey`（`solver-client.js` 已有此匯出，但目前 `ctx.solver` 沒有轉傳）。本段因分派單規定不可 `require` 除 `notation.js` 以外的其他 `src/ui/` 檔案，暫以 `hint-view.js` 內部複刻的 `mapFailureCode()`（規則與 `T-001-S6.md` 記載的對照表一致）解決，功能正確，但屬重複邏輯，日後 `solver-client.js` 若調整對照表需記得同步改這裡；補上 `ctx.solver.failureTextKey` 後即可移除本段的複刻版本，改為直接呼叫共用實作。
