# G7 回顧：魔術方塊 v0.1.0（2026-09-15～16）

> 撰寫：幕僚長。來源：docs/decision-log.md、docs/reports/T-001-arch-decisions.md（D-1～D-39）、course-notes.md、各段回報、審查與 QA 報告。

## 1. 結果

- 2026-09-15 開案 → 2026-09-16 v0.1.0 上線：`https://briantsaivc-web.github.io/cube-3x3/`，並列入遊戲小站（第 7 款，分類「空間解謎」）。
- 產物：單一 `index.html` 381,059 bytes（零外部資源、可離線）；Node 測試 100 項、Playwright UI 測試 35 項 × 手機直向／iPad 橫向。
- 功能：自由練習、提示、建議解示範（自寫兩階段求解器，QTM 100 ms 預算平均 27.64 步）、層先法分段示範（8 段、11 公式）、記號小教室、完成畫面與本機紀錄。
- 製作人拍板點：G0（6 題）、G2（6 題＋補充定位＋示範範圍）、G3.5（版型 C＋記號教學補充＋3 題）、G4.5（修 E-1/E-2、步數 A）、G5（6 個 Minor 依建議）、G6（驗收通過）、上架（封面、Pages）。

## 2. 這一輪有效、下一款直接沿用

| 做法 | 證據 |
|---|---|
| 開案前先查證製作人給的資料 | 世界紀錄 3.13 秒已過時（2.76 秒）、上帝之數用「超級電腦」細節不符，都在 G0 標出 |
| 規格前先做技術探勘（spike） | 建表時間、QTM 步數分布在 G2 前就有實測數字，企劃的 UNKNOWN 變成數字 |
| 依工作性質配模型 | 調研／企劃／UI／一般前端用 sonnet，求解器／規格／審查用 opus，打包用 haiku |
| 技術裁決集中一張表 | `T-001-arch-decisions.md` D-1～D-39，每條有來源、裁決、理由 |
| 內部架構審查在外部審查之前 | 抓到 3 個 Blocker，退回 S11 修完才給外部 AI 看 |
| 外部意見一律寫腳本實證 | Gemini 4 條：2 條證實、1 條逐幀記錄未重現、1 條屬規格既定 |
| 修正必附「修正前會失敗」證據 | S12、S13 的新測試都對舊版跑過並失敗 |
| 雲端工作區用本地 git 追蹤每段 | 29 個 commit，可隨時比對、回溯（不 push） |

## 3. 踩到的坑 → 下次規則

| # | 坑 | 下次規則 |
|---|---|---|
| 1 | 製作人在已建好的資料夾內 `git clone`，產生巢狀 `cube-3x3\cube-3x3` | 指令寫成「在上一層執行」或用 `git clone <網址> .` |
| 2 | 幕僚長在製作人電腦跑 `git status` 留下鎖檔，遠端工具又無刪檔權 | AI 不在製作人電腦跑會寫入 .git 的指令；必要時用 `--no-optional-locks` |
| 3 | 子代理回報「已目視確認」，截圖其實有問題（手機提示卡壓住方塊、iPad 面板被截半） | UI 修正由幕僚長親自看截圖驗收 |
| 4 | 整合修正刪了共用函式，另一個模組還在呼叫（B-1） | 刪除或改名匯出前先 grep 全部呼叫端 |
| 5 | 主產物在 UI 空殼階段就 222 KB，300 KB 上限不夠 | 規格估大小時把「主執行緒退回」的重複模組算進去 |
| 6 | haiku 寫的審查包混入內部編號與已修問題；haiku 寫的 CHANGELOG 出現「最短路線」與編造項目；離線測試沒有真的操作 | 對外文件與驗證腳本：程式碼區塊用腳本產生；便宜模型起草的內容逐句對照來源；看腳本實際做了什麼 |
| 7 | 桌面 App 在連結資料夾內產生「Claude outputs」，被 `git add -A` 推上 GitHub | 骨架 `.gitignore` 預設排除 `Claude outputs/`、`_to_delete/` |
| 8 | 連線中斷時本機仍是舊版 | 本機同步並逐檔比對一致後，才給 push 指令 |
| 9 | 上架說明沒寫封面完整路徑，製作人找不到檔案 | 上架說明寫本機完整路徑＋GitHub 下載網址 |
| 10 | 小站先加了清單，遊戲網址還是 404（Pages 未開） | 上架順序固定：開 Pages → 確認網址能開 → 上傳封面 → 改 games.json |
| 11 | 開 Pages 後建置失敗：Jekyll 把審查包程式碼裡的 `{{`、`{%` 當樣板語法 | 單頁遊戲 repo 骨架預設放 `.nojekyll` |

## 4. v0.2 待辦（未排程）

- 程式：打亂動畫期間仍可按記號說明／示範鈕；`nearestOrient`／`snapTo` 舊介面；兩個脆弱測試；主執行緒排程遺失時第二個請求無逾時保護；UI 重複的旋轉軸表；tutorial/records 直接用全域 document；iPad 其他 <14 px 文字；手機橫向副標換行。
- 企劃：限步挑戰（可用「≤14 QTM 最少步 1 秒內」的實測能力）、互動式逐段教學、每日挑戰、群論小教室、2D 展開圖、建表期間主執行緒即時產生層先法示範。
- 驗證：iPad Safari 實機的 E-3（打亂閃一幀）觀察。

## 5. 對團隊骨架的修訂建議（下一款開案時套用）

1. repo 骨架預設加入 `.nojekyll`；`.gitignore` 加 `Claude outputs/`、`_to_delete/`。
2. CLAUDE.md §5 加兩條：「UI 修正須附幕僚長親看的截圖」「刪除匯出前先 grep 呼叫端」。
3. release 技能加「上架清單」：Pages 開啟與網址確認 → 封面（完整路徑）→ games.json → 小站 validate.py。
4. review-packager 改用腳本產生審查包（本案 `scratch/s10/gen.py` 的做法），模型只寫說明文字。
5. 求解器這類演算法密集的案子，保留「spike 在 G2 前」的做法。
