# 課程素材：第三款「魔術方塊」過程紀錄（給 course-recorder）

- 2026-09-15 G0：製作人以「影片截圖＋一段知識描述＋一句需求」開案；幕僚長先查證其中事實（世界紀錄 3.13 秒已過時 → 2.76 秒；上帝之數用「超級電腦」實為 Google 閒置算力），示範「給 AI 的資料也要查證」。
- G0 拍板以「分號串答案」一次回覆 6 題（`網址;A;自己寫;…`），低負擔拍板技巧。
- G1 調研與 G2 企劃、求解器 spike 並行：不同子代理寫不同資料夾，依內容分配模型（sonnet 調研／企劃、opus 寫演算法）。
- spike 的價值：在寫規格前先量測（建表 1 秒、QTM 建議解平均 27.6 步），把企劃中的 UNKNOWN 變成數字；也發現企劃知識卡把「上帝之數」寫成「最少 26 步」的語意錯誤。
- 踩坑：製作人在已建立的 `C:\Users\Carrie\cube-3x3` 裡面再執行 `git clone`，產生巢狀 `cube-3x3\cube-3x3\.git`。規則：`git clone` 要在「上一層」（`C:\Users\Carrie`）執行，或在空資料夾內用 `git clone <網址> .`（結尾有一個點）。
- G2 中製作人補充定位「沒有實體方塊的人可以玩、也可以看電腦怎麼解（學習用）」→ 幕僚長指出電腦建議解人學不會，以單題選擇讓製作人決定「兩種示範都做」。
- 踩坑（AI 端）：幕僚長在製作人電腦上執行 `git status` 檢查，git 產生 `.git/index.lock`，但遠端工具沒有刪檔權限，鎖檔留著會擋住之後的 commit。處理：把鎖檔移到 `_to_delete/`（已列入 .gitignore），請製作人手動刪除。規則：**AI 不在製作人電腦上跑任何 git 指令**，git 一律由製作人執行，AI 只看檔案。
- G4 並行分工：S1/S2 → S3/S4/S5 → S6 → S7 → S8a → S8b/S8c/S8d → S8e 整合修正 → S9。每段完成後幕僚長自跑測試並抽看截圖，技術裁決集中寫在 `docs/reports/T-001-arch-decisions.md`（D-1～D-22）。
- 踩坑（AI 端）：子代理回報「已目視確認無遮擋」，幕僚長抽看截圖卻發現手機上方塊仍被提示卡壓住。規則：**UI 修正一律由幕僚長親自看截圖驗收**，不能只看子代理的文字回報。
- 踩坑：S7 時主產物已 222 KB（求解器同時放在主程式與 Worker），原訂 300 KB 上限不夠 UI 使用 → 裁決放寬為 400 KB（D-13），並記錄理由。
- G4 結束的內部架構審查（opus、唯讀）抓到 3 個 Blocker（其中 B-1 是整合修正時刪掉共用函式造成的回歸），退回 S11 修正後，幕僚長自跑全部測試並抽看截圖才放行。規則：**刪除或改名任何匯出前先 grep 所有呼叫端**。
- 審查包踩坑：haiku 產出的審查包把內部編號（B-2、D-12）與「已修好」的問題寫進已知問題 → 幕僚長改用腳本 `scratch/s10/gen.py` 重新產生，程式碼逐行帶行號、行數與 `wc -l` 核對。規則：**要貼給外部 AI 的東西用腳本產生，不讓模型手寫程式碼區塊**。
- 本機同步踩坑：遠端工具無刪檔權限，`unzip -o` 會先刪舊檔而失敗 → 改用「比對後以內容覆寫」；覆寫前備份到 `_to_delete/backup-20260915-sync3/`。另外 Claude 桌面 App 會在連結資料夾內產生「Claude outputs」資料夾，被製作人的 `git add -A` 一起推上 GitHub → 已加入 .gitignore。
