# T-001 G4 期間的技術裁決（systems-engineer／幕僚長）

| 編號 | 來源 | 問題 | 裁決 | 理由 |
|---|---|---|---|---|
| D-1 | S5 需決定 1 | 跨段相鄰整顆旋轉（hold 段 z／z′ 接 middle 段 z2）與 T-LBL-07 衝突 | 採 (a)：T-LBL-07 解讀為「同一段內不得有相鄰同字母旋轉」 | 跨段合併會破壞 hold 段完成判定；整顆旋轉不計步，對玩家無害 |
| D-2 | S5 需決定 2 | 已復原狀態仍輸出 z2 等 0 步旋轉 | 維持產生器行為；UI 在已復原時停用兩種示範鈕（S8c 實作） | 產生器保持規格定義；由 UI 避免無意義示範 |
| D-3 | S4 需決定 1 | `DEFAULT_SOLVE_OPTS` 寫在程式內 | 保留，作為防呆預設；測試已比對與 `params.json` 一致；S6 必須一律傳入 `params.solver` | 值與資料檔一致並受測試鎖定，不構成平衡數字寫死 |
| D-4 | S4 需決定 2 | T-SOL-03 鎖整張直方圖 | 保留 | 改搜尋演算法時本來就應重新檢視 |
| D-5 | S4 發現 2 | `fromStickerColors` 可能回傳 `COLOR_COUNT` | S6／UI 對未知錯誤碼一律視為非法狀態 | — |
| D-6 | S3 發現 1 | 自訂 REJECT reason 字串 | 暫不收進規格；UI 不得依字串分支，只看 REJECT 本身 | 降低耦合 |
| D-7 | 全體 | `npm run test:engine` 約 3 分鐘（T-SOL-03 佔大宗） | 接受；S9 與 CLAUDE.md §4 註明所需時間 | 1,000 局是規格驗收要求 |

幕僚長自跑驗證（2026-09-15）：`node --test "tests/engine/**/*.test.js"` → 55 pass／0 fail，約 3 分 02 秒；`check-forbidden.sh` exit 0；簡體字掃描 0 命中（「症」為繁簡共用字，誤報）。
| D-8 | S6 需決定 1 | `planCacheSize` 的單位 | 64 筆「局面 → 剩餘步驟」 | 至少完整保留兩條解，足夠 |
| D-9 | S6 需決定 2 | `NODE_LIMIT` 時是否重建 Worker | 不重建，依規格 §9.6 退回層先法；ADR §2.6 以規格為準 | 表仍有效，重建只浪費時間 |
| D-10 | S6 需決定 3 | 建表期間按層先法示範 | v0.1 依規格等建表完成；主執行緒即時產生列 v0.2 待辦 | 等待約 1–2 秒可接受 |
| D-11 | S6 需決定 4 | `createWorker(handlers)`、`deps.params` 收完整 params、選填 `deps.solvers` | 採用 | — |
| D-12 | S6 發現 | 等提示期間 reducer 仍接受 ROTATE | S8c 必須先 dispatch `HINT_REQUEST` 再以新 state 呼叫 `requestHint`；S8b 在提示等待中不送 ROTATE／SET_ORIENT；S9 新增「等提示時轉視角」測試 | 避免提示座標錯置 |
| D-13 | S7 回報 | 主產物在 UI 仍為空殼時已 221,876 bytes（含 Worker 字串 82,941 bytes 與主執行緒退回用求解器），剩餘 UI 預算不足 78 KB | 主產物上限由 300 KB 放寬為 **400 KB**；Worker 字串仍 < 100 KB | 單檔 400 KB 在 GitHub Pages 與手機載入仍屬輕量；避免 UI 為了壓大小而犧牲教學文案 |
| D-14 | 幕僚長檢視 S8a 截圖 | iPad 橫向學習面板被 `.drawer.expanded` 的 56vh 上限截在半高 | 幕僚長直接修 `src/ui/styles.css` 橫向區塊（備份 `.backup.20260915`），重跑 S8a 截圖腳本：兩視圖 0 錯誤、47 個可點元素全達標 | 小修，不影響介面契約 |
| D-15 | S8a 待決 | `DEMO_EXIT` 後自動切回「操作」分頁由誰做 | 歸 S8c（demo-player.js），透過 S8a 提供的 `ctx.panel` 切換 | 示範生命週期集中在 S8c |
| D-16 | S8a 待決 | 手機版面標籤開關位置在抽屜手把列 | 接受 | 行為與尺寸正確 |
| D-17 | S8c 發現 | 示範播完復原時，`records.js` 的全螢幕遮罩擋住「上一步／離開示範」，違反 §9.1「仍可前後單步回看」 | 交 S8e 整合修正：示範中（`state.demo` 非 null）完成畫面改為不遮擋的卡片（放在示範分頁或方塊區上緣），離開示範後不再彈出遮罩 | 規格 §9.1 |
| D-18 | 幕僚長檢視 S8c 截圖 | 手機直向的提示泡泡蓋住整個方塊，看不到層高亮 | 交 S8e：手機版提示改放在方塊區下緣或操作列上方，方塊與高亮層必須可見 | 學習用途的核心畫面 |
| D-19 | S8c／S8d 發現 | `ctx.solver` 缺 `failureTextKey`、`ctx.view` 缺面標籤變更通知、`.cube-scene` 未列入契約、`texts.hud.qtmNote` 未使用 | 交 S8e：補上介面並移除 S8c／S8d 的複製邏輯與繞道；`qtmNote` 以步數旁的小「?」或說明文字呈現；契約文件補 `.cube-scene` | 去除重複邏輯 |
| D-20 | 幕僚長檢視截圖 | 手機直向操作列換成兩行，壓縮方塊區；教學時頂面「U」標籤方向歪斜 | 交 S8e：390 寬一行放下操作列（可縮小字距或改圖示＋短字）；面標籤文字朝向調整為從目前視角可讀 | — |
| D-21 | S8b 發現 | 規格 §9.2 口語例「U5 往下＝M」在實際投影下超過 30° 門檻 | 規格口語例不改程式；列 M-03 真機手感重點 | 程式依投影判斷正確 |
| D-22 | 幕僚長檢視 S8e 截圖 | S8e 回報 D-18 已修，但手機截圖中方塊被擠到狀態列、仍被提示卡壓住（S8e 以 3D 外框估算誤判） | 幕僚長修 `styles.css`（直向時方塊區保底 210px、改由抽屜縮小讓出空間；備份 `.backup.20260915f`）；重跑 S8b、S8c、S8d、S8e 自驗腳本全過、0 JS 錯誤，截圖目視確認方塊與高亮層完整可見 | 教訓：子代理「目視確認」仍須由幕僚長抽看截圖 |
| K-1 | S9 發現 | 轉動動畫進行中點記號鍵會被忽略、不排隊；玩家快速連點 R R 時第二下可能沒反應 | 列為已知問題，交 G4.5 外部審查與 G5 QA 評估（選項：點擊排隊／縮短動畫） | 屬手感問題，非規則錯誤 |
| D-23 | 架構審查 | 3 Blocker（B-1 示範失敗路徑崩潰、B-2 視角吸附未計入目前朝向、B-3 提示卡關閉鈕 32 px）＋2 Major | **退回 G4 修正（S11）**，修完由 systems-engineer 複審後才進 G4.5 | 內部品質關卡 |
| D-24 | 審查 §11-2 | MJ-2 最佳紀錄顯示位置 | (A) 只放完成畫面；儲存不可用時不顯示（幕僚長預設，G4.5 停點時告知製作人） | 狀態列空間有限 |
| D-25 | 審查 §11-3 | MJ-1 兩個資料開關 | (A) 保留並接上程式；`|| 預設值` 改為只在鍵缺少時套用（0 為有效值）；寫死的時間與文案搬進資料檔 | 資料驅動原則 |
| D-26 | 審查 §11-4、K-1、m-2 | 動畫中點擊被丟棄；打亂與未送出的轉動競態 | **本輪一起修**，採選項 (a)：輸入佇列上限 3，取出時重檢 `canApply`；`NEW_GAME`／`RESET`／上鎖時清空（世代計數器） | 學習用途常照著記號輸入，漏轉傷害大 |
| D-27 | 審查 m-1、m-4、m-6、m-7、n-3、n-4、n-7 | engine 前置條件、提示等待中擋轉視角、完成遮罩可關閉、補測試、註解與小修 | 本輪一起修；m-5、m-8、其餘 Nit 列 v0.2 | — |
| D-28 | 審查 n-5 | `docs/spec/checks/` 與 `scratch/g3-check/` 重複 | 保留 `docs/spec/checks/` 作為版控留存（scratch 不進版控）；兩者內容相同 | — |
| D-29 | S11 需決定 | 有輔助的完成畫面是否顯示最佳紀錄；新增拒絕原因；示範失敗時不自動切回操作分頁；佇列在開始／離開示範、要求提示時也清空 | 全部採用 S11 做法；規格 §4、§6.3 已補註 | 失敗訊息必須看得到（§9.6 退回路徑） |
| D-30 | 幕僚長複審 S11 | B-1～B-3、MJ-1、MJ-2、K-1、m-2 是否確實修正 | **通過**：幕僚長自跑 engine 89/89（3 分 17 秒）、UI 測試全過（4 分 37 秒）、hook exit 0、build 371,508 bytes；審查者 5 個情境修正前後對照見 `scratch/s11-check/final.txt`；抽看完成畫面、NODE_LIMIT 退回、記號小教室截圖正常 → **可進 G4.5** | — |
| D-31 | G4.5 裁決 #2、製作人同意修 | E-2 預覽回呼過期仍重畫 | S12：`cube-view.js` 加 `previewGen` 世代計數器；新增 T-UI-31 | 舊版 12 張不符 → 新版 0 |
| D-32 | G4.5 裁決 #1、製作人同意修 | E-1 教學示範 x 只轉局部層 | S12：`previewTurn` 對整顆旋轉改用 `rotateWholeCube`；新增 T-UI-32 | 舊版 52 張在局部群組 → 新版整顆轉 |
| D-33 | 幕僚長修 E-1 時自查 | N-1：整顆旋轉（x／y／z 鍵、層先法示範的整顆轉）動畫結束時，`render()` 清 transform 卻沒關 transition，方塊反向回轉 180 ms | S12：`render()` 先設 `transition:none`；新增 T-UI-33 | 舊版動畫結束後 7 個取樣仍非單位矩陣 → 新版 0；內部發現，不經外部審查 |
| D-34 | G4.5 裁決 #4、製作人選 A | 示範步數是否計入 HUD | 維持規格 §2.2：示範步數計入本局步數（該局不列紀錄） | 製作人拍板 |
| D-35 | G5 製作人拍板（2026-09-16，「都依你建議」） | G5 六個 Minor 的處理時程 | 本版修：QA Minor-1（iPad 記號鍵副標字級）、reviewer m-2（轉視角時佇列暫停未保證解除）、m-3（主執行緒求解路徑缺錯誤與逾時保護）；下版修：m-1、m-4、m-5 | 交 S13 |
| D-36 | S13 待決（手機直向副標） | 手機直向副標是否放大 | 幕僚長依低風險格式細節直接決定：直向 11px＋左右內距 2px；390／360 寬 36 鍵皆不換行、≥44px（`scratch/s13-phone-check.js`） | 幕僚長自跑 engine 100/100、UI 35 項×2 視圖全過 |
| D-37 | S13 審查（通過，3 Minor） | m-1 主執行緒排程遺失後第二個請求無 watchdog；m-2 T-UI-36 未鎖 11px；m-3 註解過時 | m-2、m-3 幕僚長直接修（測試門檻與註解）；m-1 列 v0.2（需 0 ms 排程遺失才觸發，可按取消離開） | — |
| D-38 | 上架草稿 | 小站分類與色標 | 分類「空間解謎」、accent violet（避免與相鄰兩款 gold 連續同色）；簡介中文逗號改全形 | ui-designer 建議，幕僚長採用 |
