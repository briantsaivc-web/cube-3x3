# 裁決表：T-001 魔術方塊 v0.1（G4.5 外部交叉審查）

> 由幕僚長（兼 review-packager／systems-engineer）填寫。外部回覆原文：`external-gemini.md`。
> 判定只依可回查的證據（讀碼、規格書、實證腳本）；「外部 AI 說」不是證據。
> 製作人規則：「討論，有共識再改」——本表只裁決與實證，**尚未修改任何 `src/`**。

| 欄位 | 內容 |
|---|---|
| 任務 | T-001 |
| 審查包 | `review-pack-1.md`～`review-pack-4.md`（4 批） |
| 外部來源 | `external-gemini.md`（2026-09-16；模型版本 UNKNOWN） |
| 輪次 | 第 1 輪（最多兩輪） |
| 實證腳本 | `tests/ui/triage-g45.js`（Playwright，iPad 1194×834；不列入 smoke）；結果 `triage-results.json`（連跑兩次結果相同） |

## 1. 逐條裁決

| # | 外部意見 | 我方判定 | 理由與證據 | 建議修法 | 改動範圍 |
|---|---|---|---|---|---|
| 1 | E-1、Major、`cube-view.js:360-369`：`previewTurn` 對 x/y/z 仍用局部轉層，R／L 中心塊不跟著轉 | **需實證 → 真（採納，維持 Major）** | 實證：記號小教室第 6 步（`x`）轉動中，54 張貼紙有 52 張在轉動群組內，R 中心（13）、L 中心（40）不在（F 中心在）。截圖 `triage-e1-x-midturn.png` 可見 R 中心貼紙停在原位、周圍已轉開。外部描述「懸空脫落」略誇大（兩塊中心本來就在轉軸上，只是沒有跟著轉 90°），但這一步正是教「整顆轉」的畫面，對學習用途影響明顯。讀碼：只有 `tutorial.js` 經 `previewTurn` 播 x；示範播放器與記號鍵已用 `ctx.anim.rotate`（`demo-player.js:141,167`、`controls.js:96-105`），不受影響。 | `previewTurn` 依 `engine.kind(move)==='rotation'` 改用整顆旋轉動畫 | UI（`cube-view.js`） |
| 2 | E-2、Major、`cube-view.js:364-369`：`clearPreview()` 後，未完成的預覽回呼仍把示範貼紙畫回去 | **需實證 → 真（採納，維持 Major）** | 實證：首次教學「記號小教室 1／7」的 U 轉動進行中按「跳過教學」，1.3 秒後畫面有 **12 張貼紙與實際狀態不符**，且一直停留到玩家下一次轉動才恢復（按 R 後不符 0 張）。讀碼：`previewTurn` 的 `.then` 無條件 `paint(previewStickers)`，`tutorial.js` 的 `demoGen` 只擋下一段，擋不住已在跑的這段。玩家此時看到的方塊與提示、示範依據的狀態不同，會誤導。 | `previewTurn` 加世代計數器，`clearPreview()` 後的過期回呼直接丟棄（外部建議方向一致） | UI（`cube-view.js`） |
| 3 | E-3、Minor、`app.js:368-371`：按「打亂」時先閃一幀打亂後盤面 | **需實證 → 未重現（不採納）** | 實證：在「打亂」點擊的同一事件中以 `requestAnimationFrame` 逐幀記錄貼紙顏色（400 ms、25 幀），沒有任何一幀等於最終打亂盤面，第 1 幀亦不是。原因（讀碼）：`dispatch` 與 `previewTurn` 在同一個事件處理中同步執行，瀏覽器在兩者之間不會繪製畫面。限制：只在 Chromium 驗證，iPad Safari 未驗證（UNKNOWN，列 G5 真機觀察）。 | 無 | — |
| 4 | S-1（疑似）：示範播放時 HUD 步數跟著增加 | **不採納為 bug（符合規格）；列為設計討論** | 規格 `game-spec.md` §2.2 明定「示範中逐步播放的轉動也會進 history，也會計入本局步數；該局本來就不列紀錄」。G4 截圖亦顯示層先法示範中 HUD 隨示範增加（例：已示範 28 步時 HUD 為 28）。是否改成只算玩家自己的步數，屬設計決定。 | 由製作人決定（見第 3 節） | 若改：engine＋規格＋UI |

判定定義：採納＝可回查確認為真且修法明確；不採納＝可回查確認為假或屬規格既定；需實證＝已以腳本對決，結果填於上表。

## 2. 架構原則對照（外部意見）

外部對五條原則皆判「符合」，與內部架構審查（`docs/reports/T-001-arch-review.md`，S11 修正後複審 D-30）一致。

## 3. 需要製作人決定

1. **是否同意修 #1（E-1）、#2（E-2）**：兩項都只改 `src/ui/cube-view.js`，並補兩條 UI 測試（教學中跳過後畫面一致、x 示範時全部 54 張貼紙都在轉動群組）。修完即進 G5 QA。
2. **#4 示範步數怎麼算**：(A) 維持規格：示範的步也算進本局步數（這局本來就不列紀錄）；(B) HUD 只算玩家自己轉的步數，示範步數只顯示在示範分頁的「已示範 a／N 步」。

## 4. 製作人拍板（2026-09-16）與處理結果

- 第 1 題：**修**。S12 已修 `src/ui/cube-view.js`（E-1 整顆旋轉改用整顆轉動畫；E-2 世代計數器）。
- 第 2 題：**A**，維持規格：示範步數計入本局步數。
- 幕僚長修 E-1 時另發現 N-1（整顆旋轉動畫結束後方塊反向回轉），一併修正（D-33）。
- 回歸證據：新增 `tests/ui/checks4.js`（T-UI-31～T-UI-33），兩種視圖皆通過；同一組測試對修正前的版本（`cube-view.js.backup.20260916` 建置）全部失敗（T-UI-31 不符 12 張、T-UI-32 局部群組 52 張、T-UI-33 動畫後 7 個非單位矩陣取樣），證明測試確實抓得到問題。
- 本輪結束，進入 G5。
